apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: mlops-kubeflow-pipeline
spec:
  selector:
    app: model-serving
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
