# Full development/test environment — the union of serving, pipeline,
# and dashboard dependencies, plus test-only tooling. This is what the
# pytest suite (covering src/, app/, and dashboard/) needs installed
# locally or in CI. Never installed into a production image.
-r serving.txt
-r pipeline.txt
-r dashboard.txt

pytest==9.1.1
httpx==0.28.1
